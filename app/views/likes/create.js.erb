$(".like_link-<%= @micropost.id %>").html("<%= escape_javascript(render('microposts/unlike', micropost: @micropost)) %>");
$(".like_count-<%= @micropost.id %>").html('<%= pluralize(@micropost.likes.count, 'like') %>');
